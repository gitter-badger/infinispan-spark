package org.infinispan.spark

/**
 * @author gustavonalle
 * @since 8.0
 */
//@Test(groups = Array("functional"), testName = "spark.ReplTest")
class ReplTest /*extends MultiServerTest with RDDRetrievalTest*/ {
   //   override val cacheMode: CacheMode = CacheMode.REPL_SYNC
   //
   //   lazy val cache = remoteCache.asInstanceOf[RemoteCache[Int,String]]
   //
   //   @BeforeClass(alwaysRun = true)
   //   override def before() = {
   //      super.before()
   //      (1 to numEntries).foreach(i => cache.put(i, s"val$i"))
   //   }
}
